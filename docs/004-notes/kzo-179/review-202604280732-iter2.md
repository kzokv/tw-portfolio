# KZO-179 Code Review — Iter 2 (Delta-Only)

**Reviewer:** code-reviewer  
**Date:** 2026-04-28T07:32  
**Verdict:** ✅ PASS — no blocking findings  
**Iter 1 doc:** `docs/004-notes/kzo-179/review-202604280038-iter1.md`  
**Scope:** Delta-only pass against iter-1 finding resolutions + senior-qa-2 test files

---

## Iter 1 Finding Dispositions

| Finding | Severity | Resolution |
|---|---|---|
| H1 — Enter key fires settings-save from Accounts tab | HIGH | **FIXED** — Fix B applied (see §1) |
| M1 — auto-resolved by H1 fix | MEDIUM | **RESOLVED** |
| M2 — `accountsListSectionTitle` value Fees-scoped framing | MEDIUM | **FIXED** (see §2) |
| L2 — No `aria-labelledby` on AccountCreateForm section | LOW | **FIXED** (see §3) |
| C1 — AppShell missing `onAccountsRefresh` prop | CRITICAL | **Was false-positive** — already present at iter-1 read time (iter-2 confirms: AppShell.tsx line 866) |
| C2 — 4 missing test files | CRITICAL | **RESOLVED** — senior-qa-2 authored all 4 files (see §5) |

---

## §1 — H1 Form Restructure (SettingsDrawer.tsx + AccountCreateForm.tsx)

### SettingsDrawer.tsx

**Before (iter 1):** `form.tab === "accounts"` was grouped inside the outer settings `<form>`. Enter inside AccountCreateForm's name input triggered `form.handleSubmit()` (settings save).

**After (iter 2, lines 225–257):** Accounts tab is now a **sibling render block** — structurally identical to profile/tickers/display tabs — completely outside the outer form:

```tsx
// Line 225-255: ACCOUNTS — sibling block (not in outer form)
{form.tab === "accounts" && (
  <div className="flex min-h-0 flex-1 flex-col overflow-hidden">
    <AccountCreateForm ... />
    <AccountsListSection ... />
  </div>
)}

// Line 257: outer form now only covers general + fees
{(form.tab === "general" || form.tab === "fees") && (
  <form className="flex min-h-0 flex-1 flex-col" onSubmit={handleSubmit}>
    ...
  </form>
)}
```

✅ `accounts` removed from the outer form conditional. `UnsavedChangesFooter` only renders for `general`/`fees` — correct, since AccountCreateForm has its own independent submit action.

### AccountCreateForm.tsx

AccountCreateForm now owns an internal `<form>` (line 180) that correctly captures the Enter key:

```tsx
<section aria-labelledby={titleId} ...>
  <form className="space-y-4" onSubmit={(event) => void handleSubmit(event)}>
    ...
    <Button type="submit" ...>  {/* line 310 */}
    </Button>
  </form>
</section>
```

- `handleSubmit(event?: FormEvent<HTMLFormElement>)` at line 112 accepts optional form event, calls `event.preventDefault()` ✅
- All type pills: `type="button"` ✅ — no accidental form submission
- All currency cards: `type="button"` ✅
- Submit button: `type="submit"` (line 310) ✅ — Enter key in name input now triggers account creation, not settings save

**H1: CLEAN ✅**

---

## §2 — M2 i18n Value Correction (apps/web/features/settings/i18n.ts)

| Locale | Key | Iter 1 value | Iter 2 value |
|---|---|---|---|
| EN | `accountsListSectionTitle` | `"Account Fallback Profile"` (Fees-scoped) | `"Your accounts"` ✅ |
| zh-TW | `accountsListSectionTitle` | `"帳戶預設費率"` (correct) | `"你的帳戶"` ✅ (also updated for consistency) |

Both locales now use account-scoped framing appropriate for the new Accounts tab context.

**M2: CLEAN ✅**

---

## §3 — L2 aria-labelledby (AccountCreateForm.tsx lines 172–182)

```tsx
const titleId = "account-create-form-title";

return (
  <section
    aria-labelledby={titleId}     // ← line 176
    ...
  >
    <form ...>
      <h3 id={titleId} ...>       // ← line 182 — correctly wired
        {dict.settings.accountCreateTitle}
      </h3>
```

Section is labelled by the `<h3>` title element. Correctly wired: `id` on the heading, `aria-labelledby` on the section landmark. ✅

**L2: CLEAN ✅**

---

## §4 — data-testid="cash-ledger-account-select" (CashLedgerClient.tsx)

```tsx
// Line 242: filter toolbar container
<Card data-testid="cash-ledger-filter-toolbar">
  ...
  // Line 270: account select — correct element, correct testid
  <select ... data-testid="cash-ledger-account-select">
```

The `<select>` at line 270 is:
- Inside `data-testid="cash-ledger-filter-toolbar"` ✅ (CashLedgerPage.ts POM uses `.locate("cash-ledger-filter-toolbar").locator("select")` — compatible)
- Correct testid for direct lookup ✅
- The `filterAccountSelectIsVisible()` assert in the E2E spec will resolve correctly ✅

**Focus area 5: CLEAN ✅**

---

## §5 — Senior-QA-2 Test Files

### 5a. Suite 5 — `account-creation-uniqueness.integration.test.ts`

**Rule compliance:**

| Rule | Status |
|---|---|
| `integration-test-persistence-direct.md` — `PostgresPersistence` directly, no `buildApp` | ✅ Line 105: `new PostgresPersistence({...})` |
| Full pattern: scoped pool + `resetDatabase` + `applyNumberedMigrations` + `persistence.init()` | ✅ Lines 66-106 |
| `resolveOrCreateUser` to seed FK-compliant user | ✅ Lines 110-114 |
| `test-placement-persistence-backend.md` — 409 uniqueness test in Postgres suite | ✅ (this IS the Postgres suite) |

**Coverage (7 test cases):**

1. `ux_accounts_user_id_name` index exists with UNIQUE + correct column order ✅
2. `accounts.created_at` NOT NULL, auto-stamped within 30s of test boot ✅
3. Direct INSERT duplicate `(user_id, name)` → `23505` + `isUniqueViolation()` returns true ✅
4. `isUniqueViolation()` returns false for unrelated errors (plain Error, null, undefined, `23502`) ✅
5. Different users CAN share same account name (per-user scope) ✅
6. `saveStore()` propagates unique violation (TOCTOU safety-net path) + sanity check only one row survives ✅
7. Two concurrent INSERTs → exactly one fulfilled, one rejected with `isUniqueViolation()` true ✅
8. Happy path: fresh distinct-name insert with `created_at` stamped ✅

No `e2e-seed-vs-reset-guards.md` concerns (pure Postgres test, no HTTP endpoints). ✅

**Suite 5: CLEAN ✅**

---

### 5b. Suite 8 — `account-creation-aaa.http.spec.ts`

**Rule compliance:**

| Rule | Status |
|---|---|
| `service-error-pattern.md` — reads `body.error` not `body.code` | ✅ Line 172: `await accountsApi.assert.fieldEquals(body, "error", "fee_profile_not_found")` |
| `test-placement-persistence-backend.md` — 409 deferred to suite 5 | ✅ Top-of-file comment correctly explains with rule citation |

**Coverage (8 test cases + 2 extras):**

1. Happy path: 200 + bare AccountDto shape + fee-profile default resolution ✅
2. Default fee-profile resolution when `feeProfileId` omitted ✅
3. Explicit `feeProfileId` honored ✅
4. Empty name → 400 ✅
5. Name >80 chars → 400 ✅
6. `defaultCurrency: "EUR"` → 400 ✅
7. `accountType: "savings"` → 400 ✅
8. Missing `defaultCurrency` → 400 ✅
9. **Extra:** Invalid `feeProfileId` reference → 404 `fee_profile_not_found` (reads `body.error` ✅) — valuable extra coverage beyond test plan
10. **Extra:** Trim semantics — leading/trailing whitespace stripped before persistence ✅
11. GET /accounts round-trip reflects new account ✅

`createdAt` NOT exposed on DTO verified at line 57–59 (D2/D7 compliance check) ✅

**Suite 8: CLEAN ✅**

---

### 5c. Suite 3 — `AccountCreateForm.test.tsx`

**Pattern:** `react-dom/client` + `act()` — matches project web-unit harness ✅

**Coverage (9 test cases):**

1. Renders 4 base fields; fee-profile picker hidden when `feeProfiles.length === 1` (D5) ✅
2. Fee-profile picker renders with correct options when `feeProfiles.length > 1` ✅
3. Live-preview chip updates as name + type + currency change ✅
4. Submit disabled when name empty or whitespace-only; enabled otherwise ✅
5. Submit calls `onCreate` with trimmed input + `onAccountsRefresh` after success; form resets ✅
6. `feeProfileId` included in `onCreate` input when picker is shown ✅
7. 409 `ApiError` → `accountCreateNameInUseError` text; `onAccountsRefresh` NOT called; name NOT reset ✅
8. Non-409 failure → `accountCreateGenericError` text; `onAccountsRefresh` NOT called ✅

**Submit mechanism:** Button is `type="submit"` inside the component's own `<form>`. In JSDOM (Vitest + react-dom/client), `submit.click()` triggers the form's `submit` event, which React handles via `onSubmit`. `handleSubmit(event)` calls `event.preventDefault()`. This chain is standard JSDOM behavior and works correctly.

**Suite 3: CLEAN ✅**

---

### 5d. Suite 6 — `account-creation-aaa.spec.ts`

**Rule compliance:**

| Rule | Status |
|---|---|
| `e2e-aaa-guardrails.md` — no `networkidle`, deterministic waits | ✅ Only element waits used |
| `playwright-web-bundle-rebuild.md` — note in doc comment | ✅ Line 29 |
| `e2e-seed-vs-reset-guards.md` | ✅ N/A — no `/__e2e/seed-*` calls; UI-only flow |
| D8 — one spec only | ✅ Single test |

**Flow:** open drawer → Accounts tab → fill name/type/currency → assert preview chip → submit → assert form reset (D12) → assert drawer list shows 2 accounts → close → navigate `/cash-ledger` → assert filter toolbar + account select visible → round-trip drawer re-open → assert persistence. ✅

**D8 dropdown chip:** Correctly implemented as interpretation (B) per architect [QUESTION] during EXECUTE. Comment explains why newly-created accounts don't appear as cash-ledger filter options (filter derives from accounts with cash entries; fresh account has none). Assertion limited to "page renders cleanly" — valid and correct. ✅

**`submitAccountCreate()`:** Awaits `POST /accounts` response with `response.ok()` check (SettingsActions.ts lines 139–148) — prevents the next assertion from running before the server confirms the account was created. ✅

**Suite 6: CLEAN ✅**

---

### 5e. POM Changes

| File | Changes | Status |
|---|---|---|
| `SettingsDrawerPage.ts` | `tabs.accounts` locator; `accountCreate` element group (form, nameInput, typePill, currencyCard, feeProfileSelect, previewChip, submit, error) | ✅ |
| `SettingsActions.ts` | `fillAccountCreateName`, `selectAccountCreateType`, `selectAccountCreateCurrency`, `submitAccountCreate` (awaits POST /accounts response) | ✅ |
| `SettingsAssert.ts` | `accountNameLabelCountIs`, `accountNameLabelContains`, `accountCreateFormIsVisible`, `accountCreatePreviewContains`, `accountCreateNameInputIsEmpty` | ✅ |
| `SettingsArrange.ts` | `openAccountsTab` | ✅ |
| `CashLedgerPage.ts` | `filterToolbar` (`cash-ledger-filter-toolbar`), `filterAccountSelect` (select within filter toolbar) | ✅ — `data-testid="cash-ledger-filter-toolbar"` confirmed at CashLedgerClient.tsx:242; select at line 266 is inside it |
| `CashLedgerAssert.ts` | `filterToolbarIsVisible`, `filterAccountSelectIsVisible` | ✅ |

All testid targets confirmed against source components. All `@Step()` decorators present. ✅

---

### 5f. test-api-mapper-registration.md

```ts
// libs/test-api/src/config/mapper.ts
import { AccountsEndpoint } from "../endpoints/AccountsEndpoint.js";  // line 15
apiAssistantRegistry.register(AccountsEndpoint, accountsApiAssistantFactory);  // line 36
```

AccountsEndpoint registered before any test that calls `accountsApi.actions.createAccount(...)`. ✅

---

### 5g. account-display-aaa.spec.ts (modified)

The existing spec was updated to reflect D1 tab relocation: `AccountsListSection` moved from Fees → Accounts tab. The spec now calls `settings.arrange.openAccountsTab()` (line 17) instead of the previous Fees tab navigation. KZO-179 comment at line 16 explains the relocation. All other assertions unchanged. ✅

---

## Informational (no action required)

**I1 — Dead CSS class on AccountCreateForm `<section>`:** `<section className="glass-inset space-y-4 rounded-[24px] p-4">` has `space-y-4`, but the section's only direct child is `<form>` (a single child). `space-y-4` requires multiple siblings to activate — it has no visual effect here. Zero functional impact; cosmetic dead class.

**I2 — `account-display-aaa.spec.ts` uses raw `page.getByTestId()` for rename interactions:** Pre-existing style in the spec (rename flow was authored before the AAA POM extension). Not a KZO-179 regression; rename POM extension is out of scope.

---

## Rule-by-Rule Compliance Table (delta scope)

| Rule | Disposition |
|---|---|
| `integration-test-persistence-direct.md` | ✅ PASS — PostgresPersistence directly, full pattern, no buildApp |
| `test-placement-persistence-backend.md` | ✅ PASS — 409 uniqueness in suite 5, not suite 8 |
| `test-api-mapper-registration.md` | ✅ PASS — AccountsEndpoint registered in mapper.ts |
| `e2e-seed-vs-reset-guards.md` | ✅ N/A — no E2E seed endpoints in new specs |
| `service-error-pattern.md` | ✅ PASS — suite 8 reads `body.error`, not `body.code` |
| `nextjs-i18n-serialization.md` | ✅ PASS (confirmed in iter 1; unchanged) |
| `e2e-aaa-guardrails.md` | ✅ PASS — no networkidle, element waits, 2-worker default |
| `playwright-web-bundle-rebuild.md` | ✅ PASS — noted in E2E spec header comment |

---

## Verdict

**PASS** — all iter-1 HIGH/MEDIUM/LOW findings resolved. No new blocking findings. Test coverage is complete across suites 3, 5, 6, 8. POM extensions are correctly wired to source testids. All rule-compliance checks pass.

The codebase is ready for Validator's 8-suite gate.
