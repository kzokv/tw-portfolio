# KZO-179 Wave 2 Compliance Review — PR Body + Transition Note

**Reviewer:** code-reviewer  
**Date:** 2026-04-28T08:19  
**Scope:** Wave 2 artifacts — PR description draft + transition note  
**Artifacts reviewed:**
- `.worklog/team/pr-description-draft.md` (PR body)
- `docs/004-notes/kzo-179/transition-202604280012-multi-account-creation.md`

**Compliance reference:** `pr-bound-docs-review-compliance.md`, `git-pr-flow.md §3-4`

---

## Verdict: ⚠️ FINDINGS — 1 MEDIUM

One MEDIUM finding (PR body `## Solution` missing a behavioral-delta callout). All CI-gate structural requirements otherwise satisfied. Fix is a single sentence.

---

## §1 — PR Body Structural Compliance (`pr-gate.yml` sections)

| Required section | Present? | Notes |
|---|---|---|
| `## Summary` | ✅ | Line 7 |
| `## Problem` | ✅ | Line 13 — separate from Summary, describes KZO-167 deferred gap |
| `## Solution` | ✅ | Line 31 — 7 bullets covering route, tab, form, migration, exports, tests |
| `## Testing` | ✅ | Line 43 — correct heading (NOT `## Test Coverage`) |
| `Evidence:` block | ✅ | Lines 47–56 — all 8 suites with concrete pass/skip/fail counts |
| `## Risk/Rollback` | ✅ | Line 60 — schema risk, behavioral risk H1 Fix B, monitoring guidance |

All four CI-gate headings are present and correctly named. ✅

---

## §2 — Behavioral-Delta Callouts (per architect brief)

### 2a. `created_at` NOT on `AccountDto` (D2/D7 alignment)

> `## Risk/Rollback` line 62: "Safe because no production code currently reads `created_at` from the DB — the column is not on `AccountDto` and not queried anywhere."

Explicitly called out in `## Risk/Rollback`. ✅

---

### 2b. H1 Fix B unsaved-state degradation (`UnsavedChangesFooter` scoping)

> `## Risk/Rollback` line 64: "Side effect: fee-profile binding edits and renames in `AccountsListSection` mutate `form.draft.accounts` but the Save bar is not visible while on the Accounts tab... Functional; noted as follow-up in KZO-181 territory."

Explicitly acknowledged as an intentional trade-off, not a regression. ✅

---

### 2c. Suite 7 `[timeframe-Q]` pre-existing failure

> `## Testing` line 54: "Suite 7 (E2E OAuth): 82 passed, 1 failed — `[timeframe-Q]` in `dashboard-timeframe-aaa.spec.ts` is a pre-existing KZO-161 spec bug (structural `afterEach` viewport-restore race); NOT a KZO-179 regression; documented in transition note."

Explicitly called out as "NOT a KZO-179 regression." ✅

---

### 2d. `data-testid="cash-ledger-account-select"` production attribute add

**MEDIUM — NOT PRESENT IN `## Solution`**

The `data-testid="cash-ledger-account-select"` attribute added at `CashLedgerClient.tsx:270` is a production-code change, not a test-only artifact. It is the testid wired into `CashLedgerPage.ts` and used by `CashLedgerAssert.ts`. Its absence from the PR body `## Solution` leaves reviewers unable to identify the scope of CashLedgerClient.tsx changes from the PR body alone.

The attribute appears in `transition-202604280012-multi-account-creation.md §5` — but transition notes are read post-merge; the PR body is the point-of-review record.

**Required fix:** Add one sentence to `## Solution` (e.g. as a line in the existing bullet 1 or 3, or as an addendum to bullet 3):

> `CashLedgerClient.tsx:270` gains `data-testid="cash-ledger-account-select"` on the filter `<select>` (production attribute — wires the cash-ledger POM locator). `data-testid="cash-ledger-filter-toolbar"` already present on the containing `<Card>` at line 242.

This is a structural compliance finding: the PR body must enumerate every production-file behavioral delta so the reviewer and CI-gate record is complete.

**Severity: MEDIUM** — not CI-blocking on submission (pr-gate.yml validates section headings, not content completeness), but blocks the "reviewer can confirm scope from the PR body alone" contract per `pr-bound-docs-review-compliance.md`.

---

## §3 — Transition Note Completeness

| Expected section | Present? |
|---|---|
| TL;DR / overview | ✅ |
| Behavioral Changes §1–5 | ✅ — Tab relocation (§1), AccountCreateForm (§2), migration 041 (§3), role guards (§4), cash-ledger testid (§5) |
| Migration 041 detail | ✅ — `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` + `ux_accounts_user_id_name` rollback guidance |
| Explicit No-ops (7 items) | ✅ — `created_at` not on DTO, PATCH routes unchanged, existing account unaffected, etc. |
| Cross-cutting Carve-outs | ✅ — Suite 7 `[timeframe-Q]`, H1 Fix B unsaved-state, cash-ledger filter dropdown sourcing |
| Forward Links table | ✅ — KZO-168 (FX Transfer), KZO-181 (binding PATCH) |
| References | ✅ — includes iter-2 CR doc `review-202604280732-iter2.md` |

Transition note is **complete**. All required behavioral-delta entries are present and correctly framed. ✅

The `data-testid="cash-ledger-account-select"` add is documented in §5 of the transition note — the gap is specifically in the PR body `## Solution`, not the transition note.

---

## §4 — Renamed Types / Classes Table

PR body lines 19–28: 3-row table present —

| Old name | New name | Location |
|---|---|---|
| `AccountFallbackSection` (component) | `AccountsListSection` | `apps/web/features/settings/components/` |
| `accountFallbackSectionTitle` (i18n key) | `accountsListSectionTitle` | both i18n files |
| `accountFallbackSectionDescription` (i18n key) | `accountsListSectionDescription` | both i18n files |

Zero-external-callers confirmation present. ✅

---

## §5 — Rule-by-Rule Compliance

| Rule | Status |
|---|---|
| `pr-bound-docs-review-compliance.md` — `## Problem` present | ✅ |
| `pr-bound-docs-review-compliance.md` — `## Solution` present | ✅ (content gap: see §2d) |
| `pr-bound-docs-review-compliance.md` — `## Testing` with `Evidence:` block | ✅ |
| `pr-bound-docs-review-compliance.md` — `## Risk/Rollback` present | ✅ |
| `pr-bound-docs-review-compliance.md` — behavioral deltas called out | ⚠️ MEDIUM — testid add missing from `## Solution` |
| `process-refactor-rename-verification.md` — renamed types table | ✅ |
| `doc-management.md` — transition note frozen, datetime-named | ✅ |

---

## Required Fix

**Technical-writer action required:**

In `.worklog/team/pr-description-draft.md` `## Solution` section, add mention of `data-testid="cash-ledger-account-select"` production attribute add. Suggested placement: append to bullet 3 (AccountCreateForm) or bullet 5 (Migration 041), or as a standalone addendum line. The information is already correct in `transition-202604280012-multi-account-creation.md §5` — this is a copy task, not new research.

After fix: re-route to code-reviewer for a single-sentence re-pass.
