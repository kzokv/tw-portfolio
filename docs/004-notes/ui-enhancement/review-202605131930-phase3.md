# Phase 3 Code Review — ui-enhancement Wave 1

**Reviewer:** code-reviewer (Sonnet, Tier 3)
**Date:** 2026-05-13T19:30 UTC
**Branch:** `worktree-ui-enhancement`
**Scope:** Wave 1 deliverables — account soft-delete/restore/hard-purge lifecycle, fee/tax 4-tuple render gate, market chip ALL removal
**Authorized by:** `[ARCHITECT:GO]` envelope (12-item targeted checklist)

---

## Verdict: CLEAN

All 12 checklist items pass. No HIGH or CRITICAL findings. Zero FIX-REQUIRED items.

---

## Checklist Results

### 1. Cascade order in `hardPurgeAccount` — CLEAN ✓

`apps/api/src/persistence/postgres.ts` `hardPurgeAccount` method (line 7729) implements the full 12-table cascade in correct dependency order matching architect-design.md §5:

1. audit_log row written BEFORE deletion (snapshot captured)
2. trade_fee_policy_snapshots — intentionally LEFT (documented comment; user-scoped orphan per §5 note)
3. dividend_ledger_entries
4. dividend_payment_records
5. corporate_actions
6. account_fee_profile_overrides (via CASCADE from fee_profiles)
7. fee_profiles (with account_fee_profile_overrides via CASCADE)
8. currency_wallet_snapshots
9. daily_holding_snapshots
10. lot_allocations
11. lots
12. cash_ledger_entries
13. trade_events
14. accounts row (final DELETE)
15. COMMIT

All operations in a single transaction. `routeError` used (not raw `Error`). No `saveStore` pattern.

**Memory.ts** `hardPurgeAccount` also handles both soft-deleted and active accounts via `mustBeSoftDeleted` flag.

---

### 2. Active-only filter for read paths — CLEAN ✓

**`getAccounts`** (postgres.ts:1745): `WHERE user_id = $1 AND deleted_at IS NULL` ✓

**`loadStore`** (postgres.ts:1745): accounts query includes `AND deleted_at IS NULL` — comment documents the filter addition. All downstream consumers of `loadStore` (dashboard, share-view, snapshots) naturally see only active accounts.

**`saveStore`** (postgres.ts:2321): DELETE cleanup guard is `AND deleted_at IS NULL` — soft-deleted accounts are not touched by the store-replace operation. ✓

**fee_profiles in loadStore**: Loaded for all accounts (including soft-deleted) via JOIN on `user_id`. This is intentional per scope item 12 ("filter-on-read; do not recompute historical snapshots") — fee profiles must be preserved for potential restoration. Not a finding.

**`listUserAccountIds`** (postgres.ts:5174): Not filtered by `deleted_at` — intentional; used in write paths (`saveAccountingStore`) where accounting data for soft-deleted accounts must be preserved.

---

### 3. SSE event union sync — CLEAN ✓

`libs/shared-types/src/events.ts`:
- `AccountSoftDeletedEvent`, `AccountRestoredEvent`, `AccountHardPurgedEvent` interfaces defined (lines 174–189) ✓
- `SSEEvent` discriminated union includes all 3 (lines 212–214) ✓
- `SSEDomainEventType` string-literal union includes all 3 (lines 236–238) ✓

Both union sites updated — no drift between the two.

---

### 4. env-metadata 3-location update — CLEAN ✓

`libs/config/src/env-metadata.ts`:
- `envGroups[Security/Tuning]`: `ACCOUNT_HARD_PURGE_CRON` and `ACCOUNT_HARD_PURGE_DAYS` present ✓
- `dockerCloudGroups[Application]` (lines 77–78) ✓
- `dockerLocalGroups[Application]` (lines 121–122) ✓

All 3 mandatory locations confirmed.

---

### 5. app_config integration — CLEAN ✓

**Cache** (`apps/api/src/services/appConfig/cache.ts`):
- `AppConfigCacheEntry.accountHardPurgeDays: number | null` (line 79) ✓
- Generation counter initialized to 0, `invalidate()` bumps +1 (line 210) ✓
- `refresh()` captures `startGeneration` and discards stale results via `_state.generation !== startGeneration` (lines 172–173) ✓

**Bounds** (`bounds.ts`): `accountHardPurgeDays: { min: 1, max: 365 }` (line 71) ✓

**Types** (`types.ts`): `AppConfigPlainField` union includes `"accountHardPurgeDays"` (line 63); `APP_CONFIG_PLAIN_COLUMNS` maps to `"account_hard_purge_days"` (line 110); `AppConfigDto.accountHardPurgeDays: number | null` (line 1032) ✓

**Resolver** (`accountLifecycle.ts`): `getEffectiveAccountHardPurgeDays()` reads cache then falls back to `Env.ACCOUNT_HARD_PURGE_DAYS` ✓

**Sweep-parameter-live** (`registerAccountHardPurgeWorker.ts`): `graceDays` read AT TICK TIME inside the handler (line 49) — not captured at registration ✓

**PATCH-response from post-write row** (`adminRoutes.ts`): `loadAppConfigDto` calls `app.persistence.getAppConfig()` directly (not the cache), then calls `buildAppConfigDtoFromRow` with inline `row.accountHardPurgeDays ?? Env.ACCOUNT_HARD_PURGE_DAYS`. PATCH handler calls `invalidate()` then `loadAppConfigDto(app)` — response never reads from cache ✓

**`_helpers.ts`** (appConfig test helper): `accountHardPurgeDays: null` included in `fakePersistenceWithAppConfig` row shape (line 65) ✓

---

### 6. Locked testid match (architect-design.md §9) — CLEAN ✓

Verified all locked strings against source:

| Component | Locked testid | Source | Match |
|---|---|---|---|
| `AccountsListSection` | `account-delete-btn-{id}` | line 630 | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-modal` | present | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-warnings` | present | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-warning-open-positions` | present | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-warning-cash-balance` | present | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-warning-last-account` | present | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-cancel-btn` | present | ✓ |
| `AccountSoftDeleteModal` | `account-soft-delete-confirm-btn` | present | ✓ |
| `AccountPermanentDeleteModal` | `account-permanent-delete-modal` | present | ✓ |
| `AccountPermanentDeleteModal` | `account-permanent-delete-input` | present | ✓ |
| `AccountPermanentDeleteModal` | `account-permanent-delete-cancel-btn` | present | ✓ |
| `AccountPermanentDeleteModal` | `account-permanent-delete-confirm-btn` | present | ✓ |
| `AccountsListSection` | `recently-deleted-section` | line 1104 | ✓ |
| `AccountsListSection` | `recently-deleted-header` | line 1106 | ✓ |
| `AccountsListSection` | `recently-deleted-row-{id}` | line 1122 | ✓ |
| `AccountsListSection` | `recently-deleted-time-remaining-{id}` | line 1128 | ✓ |
| `AccountsListSection` | `recently-deleted-restore-btn-{id}` | line 1143 | ✓ |
| `AccountsListSection` | `recently-deleted-purge-btn-{id}` | line 1153 | ✓ |
| `AddTransactionCard` | `tx-market-chip-{TW\|US\|AU}` | line 350 | ✓ |
| `AddTransactionCard` | `tx-market-chip-ALL` | ABSENT from source | ✓ |

Page-object `SettingsDrawerPage.ts` locators (lines 259–322) match all source testids exactly.

---

### 7. i18n flat-Record discipline — CLEAN ✓

`apps/web/lib/i18n/types.ts` (lines 460–487): All 17 new `dict.settings.*` entries are declared as `string` (flat, no nested Records, no functions).

`apps/web/features/settings/i18n.ts`: All values are string literals or `{placeholder}` token strings. No function values. Both `en` and `zh-TW` locales updated.

Call-site pattern verified:
- `dict.settings.accountsRecentlyDeletedTitle.replace("{count}", String(n))` at AccountsListSection.tsx:1108 ✓
- `dict.settings.accountsTimeRemaining.replace("{days}", ...)` at AccountsListSection.tsx:1130 ✓
- `dict.settings.accountsPermanentDeleteInputLabel.replace("{name}", ...)` at AccountPermanentDeleteModal.tsx:79 ✓

Compliant with `i18n-flat-record-dict-settings.md` and `nextjs-i18n-serialization.md`.

---

### 8. Migration immutability — CLEAN ✓

`db/migrations/053_uie_accounts_deleted_at.sql` (new):
- `ALTER TABLE accounts ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ NULL` ✓
- Drops full unique index `ux_accounts_user_id_name` and replaces with partial `ux_accounts_user_id_name_active` (WHERE `deleted_at IS NULL`) ✓
- `CREATE INDEX IF NOT EXISTS idx_accounts_deleted_at` for cron query performance ✓

`db/migrations/054_uie_app_config_account_hard_purge_days.sql` (new):
- `ALTER TABLE public.app_config ADD COLUMN IF NOT EXISTS account_hard_purge_days INT NULL` ✓
- COMMENT present ✓

No existing migration files were modified. Both are net-new sequential files with IF NOT EXISTS guards.

---

### 9. `as never` audit — CLEAN ✓

Grepped `apps/api/test/` for `as never` / `as unknown as` casts. Findings:

- **`account-hard-purge-worker.test.ts`**: 4 narrow cast sites, each stubbing only `selectAccountsForHardPurge` and `hardPurgeAccount` (the methods the cron worker calls). Correct narrowly-scoped pattern.
- **`registerAnonymousShareTokenPurgeWorker.test.ts`**: Casts the entire `deps` object; stubs only `purgeTerminalAnonymousShareTokens`. Pre-existing file; no new Persistence methods needed.
- **`appConfig/_helpers.ts`**: `fakePersistenceWithAppConfig` row shape includes `accountHardPurgeDays: null` (line 65). ✓
- **Integration/HTTP specs**: Use real `MemoryPersistence` / `PostgresPersistence` instances — TypeScript would fail at compilation if any interface methods were missing.

No cast sites found that would silently suppress missing new Persistence methods.

---

### 10. `libs/test-api` mapper registration — CLEAN ✓

`libs/test-api/src/config/mapper.ts` (line 46): `AccountsEndpoint` already registered.

`libs/test-api/src/endpoints/AccountsEndpoint.ts` (lines 29–58): 4 new methods added to existing `AccountsEndpoint` class:
- `softDelete(accountId)` → `DELETE /accounts/:id` ✓
- `restore(accountId)` → `POST /accounts/:id/restore` ✓
- `purge(accountId, payload)` → `POST /accounts/:id/purge` ✓
- `listDeleted(headers?)` → `GET /accounts/deleted` ✓

No new endpoint class required — methods extend existing class. Registration via the existing mapper entry is sufficient.

---

### 11. FE flag — KZO-169 one-way binding / scope item 22 — CLEAN ✓

The Frontend Implementer correctly reversed the chip→account filter direction from KZO-169.

**Source (`AddTransactionCard.tsx`, lines 192–198)**: Comment explicitly documents: "Previously (KZO-169) the dropdown was filtered to currency-compatible accounts (`chip → account`); that direction is now removed. The dropdown lists ALL of the user's accounts; picking one drives the chip via the auto-sync effect below."

**One-way binding** (lines 219–246): `useEffect` on `value.accountId` change — when account changes:
1. `setExplicitChip(nextMarket)` — chip auto-syncs ✓
2. `ticker: ""` — ticker field clears ✓
3. `priceCurrency: currencyFor(nextMarket)` — currency reconciles ✓

**Chip remains user-changeable**: `handleChipChange` callback still controls user-driven chip changes. Scope item 22 satisfied.

**Unit tests** (`AddTransactionCard.test.tsx`):
- Line 186: new test "shows the account dropdown with ALL accounts regardless of chip (ui-enhancement)" — explicitly replaces KZO-169 filter behavior ✓
- Line 161: "renders the inline no-account error block ONLY in the zero-account state" — updated semantics ✓

This is intentional, semantically correct, and the unit tests have been updated consistently. NOT a regression.

---

### 12. Stale-artifact warning — APPLIED ✓

All greps during this review were scoped to `apps/`, `libs/`, `db/migrations/` source paths. No `.next/`, `dist/`, or other build-output directories were consulted.

---

## Informational Notes (non-findings)

**ACCDEL ticker registration deferred to Wave 2**: `ACCDEL01`–`ACCDEL05` tickers referenced in E2E specs with comments "per `e2e-shared-memory-bars-ticker-hygiene.md`". Rule file does not yet list them. **architect-design.md §11 explicitly says**: "Append the reserved list to `e2e-shared-memory-bars-ticker-hygiene.md` as part of Wave-2 docs." This is a known planned Wave 2 deliverable, not a gap.

**fee_profiles for soft-deleted accounts in `loadStore`**: The fee_profiles JOIN does not filter by `deleted_at IS NULL`. This is intentional — fee profiles are preserved for account restoration per scope item 12. No correctness issue.

---

## Finding Count by Severity

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| Informational | 2 |

---

## Conclusion

Wave 1 implementation is clean across all 12 targeted items. Ready for exit check. No Phase 4 work required from this review.
