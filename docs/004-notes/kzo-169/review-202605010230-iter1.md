# KZO-169 Pre-PR Review - Iteration 1

## Verdict

No open blocking findings after final handoff review.

## Findings

- Resolved during review: memory persistence still had an application-side `?? "TW"` fallback for position-derived monitored tickers. It now fails loudly with `market_code_missing` when the source trade event lacks `marketCode`, and the unit fixtures seed the source trade event with `marketCode`.

## Scope Checks

- Schema, persistence, API, UI, settings, tests, and notes for KZO-169 are delivered.
- Provider-stamping audit: no application-side `?? "TW"` fallback remains in the touched API persistence path; migration-only `DEFAULT 'TW'` remains for existing-row backfill.
- Typecheck coverage: root typecheck includes API tests, API integration tests, API unit tests, web/app/lib builds, and shared test libs.
- Automated review tooling was attempted, but its default branch comparison was not useful for scoped findings because this worktree's changes are uncommitted and the analyzer compared the historical branch to `main`.

## Testing

Evidence:

- `npx eslint . --max-warnings=0`
- `npm run typecheck`
- `npm run test:all:full`

Final full-gate phase results:

- Web unit: 47 files passed, 581 tests passed.
- API managed Postgres integration: 52 files passed, 579 tests passed, 1 skipped.
- Bypass E2E: 190 tests passed.
- OAuth E2E: 87 tests passed.
- API HTTP: 185 tests passed, 2 skipped.

Focused pre-gate checks:

- `npm run test --prefix apps/api -- --run monitored-tickers backfill-retry`
- `npm run test:e2e:bypass:mem --prefix apps/web -- tests/e2e/specs/tooltips-a11y-aaa.spec.ts`
