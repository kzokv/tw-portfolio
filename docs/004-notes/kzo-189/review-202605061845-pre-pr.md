---
slug: kzo-189
type: review
created: 2026-05-06T18:45:00Z
reviewer: code-reviewer
iteration: 1
verdict: CLEAN
---

# Code Review — KZO-189 Pre-PR (Iteration 1)

**Verdict: CLEAN** — 0 Critical · 0 High · 0 Medium · 1 Low · 5 Informational

No blocking findings. No action required before PR creation.

---

## CRITICAL — All Pass

### C1 — `RateLimitedError` re-throw preserved inside `if (shouldEnrich)` block
**Rule:** `typed-transient-error-catch-audit.md`  
**File:** `apps/api/src/services/market-data/backfillWorker.ts:251–272`  
**Status: PASS**

The metadata enrichment `try/catch` block at lines 253–271 is entirely inside `if (shouldEnrich) { ... }` (line 251). The re-throw at line 264 is:

```ts
if (shouldEnrich) {
  const catalogProvider = catalogRegistry.get(market);
  if (catalogProvider) {
    try {
      ...
    } catch (metaErr) {
      if (metaErr instanceof RateLimitedError) {
        throw metaErr; // line 264 — INSIDE if(shouldEnrich) ✓
      }
      log.warn(...);  // warn-and-continue only for non-rate-limit errors
    }
  }
}
```

The outer `catch (err)` at line 326 catches the re-thrown `RateLimitedError` and calls `rescheduleAfterRateLimit`. The rescue path is preserved exactly. ✓

### C2 — Migration is new sequential file, not an in-place edit
**Rule:** `migration-strategy.md`  
**File:** `db/migrations/045_kzo189_metadata_enrichment_mode.sql`  
**Status: PASS**

New file `045_kzo189_metadata_enrichment_mode.sql`. Does not edit any prior migration. Uses `ADD COLUMN IF NOT EXISTS` (safe for re-run), drops stale constraint name before adding (idempotent). CHECK constraint values match the `"unconditional" | "conditional"` enum exactly. ✓

---

## HIGH — All Pass

### H1 — Interface caller verification: both new persistence methods have callers
**Rule:** `interface-caller-verification.md`  
**Files:** `apps/api/src/persistence/types.ts:784,789`  
**Status: PASS**

Grep verification:
- `getMetadataEnrichmentMode` → called from `apps/api/src/services/market-data/metadataEnrichmentMode.ts:14` via `persistence.getMetadataEnrichmentMode()`. Also exercised by integration test and the `getAppConfig()` in postgres.ts. ✓
- `setMetadataEnrichmentMode` → called from `apps/api/src/routes/adminRoutes.ts:406` in PATCH handler. ✓

Both methods have at least one production caller. ✓

### H2 — Shared-types barrel Turbopack safety
**Rule:** `shared-types-barrel-turbopack.md`  
**File:** `libs/shared-types/src/index.ts:504–510`  
**Status: PASS**

`AppConfigDto` additions (`metadataEnrichmentMode`, `effectiveMetadataEnrichmentMode`) are type-only interface field additions. The barrel already contains runtime value exports (`MARKET_CURRENCY_PAIRS`, `marketCodeFor`, `currencyFor` at lines 100–128) so Turbopack already resolves it as a real module. No new `export *` from a pure-type submodule introduced. Sibling `export type * from "./events.js"` unchanged. ✓

### H3 — service-error-pattern: admin route validation uses correct error path
**Rule:** `service-error-pattern.md`  
**Files:** `apps/api/src/routes/adminRoutes.ts:57–61, 364`  
**Status: PASS**

`patchAdminSettingsSchema` extends with `metadataEnrichmentMode: z.union([z.enum(["unconditional", "conditional"]), z.null()]).optional()`. A value like `"foo"` throws a Zod validation error which Fastify's error handler converts to `{ error: "validation_error", ... }` (400). Explicit errors use `routeError()` throughout (403, 404, 409, 503 cases). No plain `throw new Error()` in the admin settings PATCH block. ✓

### H4 — TS2552 cross-task scope issue resolved
**Cross-task finding from Implementer:** QA's `describe("metadata enrichment gate")` block initially placed at module top-level, outside the parent describe where `createAuDeps()` is defined.  
**Status: PASS (resolved)**

Verified: `describe("metadata enrichment gate (KZO-189)", ...)` at line 801 is a sibling of `createAuDeps` (line 592) within the same parent `describe` block (closes at line 898). Both are at 2-space indentation inside the parent. `createAuDeps` is in scope at all four `it` call sites. ✓

---

## MEDIUM — All Pass

### M1 — Playwright testid drift: locators match component testids
**Rule:** `playwright-page-object-testid-drift.md`  
**Files:** `libs/test-e2e/src/pages/layout/AppShellPage.ts:91,95,99` vs `apps/web/components/admin/AdminSettingsClient.tsx:560,570,601`  
**Status: PASS**

Grep verification — all 3 page-object locators have matching `data-testid` in the component:

| Locator string | Component testid | Match |
|---|---|---|
| `admin-settings-metadata-enrichment-mode-select` | `AdminSettingsClient.tsx:560` | ✓ |
| `admin-settings-metadata-enrichment-mode-effective` | `AdminSettingsClient.tsx:570` | ✓ |
| `admin-settings-metadata-enrichment-mode-save` | `AdminSettingsClient.tsx:601` | ✓ |

Additional testids in the component (`-section`, `-error`, `-success`) are NOT in the page object, but are used via `page.getByTestId()` directly in the E2E spec — which is acceptable (page-object rule only mandates drift-free locators that ARE in the page object). ✓

### M2 — Admin new subpage checklist: N/A
**Rule:** `admin-new-subpage-checklist.md`  
**Status: N/A**

No new admin subpage created. KZO-189 adds a new section to the existing `/admin/settings` page. 5-touch-point checklist does not apply. ✓

### M3 — Integration test uses PostgresPersistence directly; seeds real actor user
**Rule:** `integration-test-persistence-direct.md`  
**File:** `apps/api/test/integration/admin-settings-metadata-enrichment-mode.integration.test.ts`  
**Status: PASS**

Follows the "full pattern" from the rule:
- Creates `Pool` + calls `resetDatabase()` + `applyNumberedMigrations()` ✓
- Instantiates `PostgresPersistence({ databaseUrl, redisUrl })` directly ✓
- Seeds real actor user via `persistence.resolveOrCreateUser("google", ...)` → `adminActorId` ✓
- Uses `adminActorId` in `appendAuditLog` calls (FK constraint satisfied) ✓
- `describePostgres` pattern correctly gates on `RUN_POSTGRES_INTEGRATION` ✓

### M4 — Vitest mock pattern: no Env scalar mocking without method closure replacement
**Rule:** `vitest-config-patterns.md`  
**Status: PASS**

No `vi.mock("@tw-portfolio/config")` for `METADATA_ENRICHMENT_MODE` in any test. The unit tests inject the functor via `BackfillWorkerDeps.getEffectiveMetadataEnrichmentMode: vi.fn().mockResolvedValue("conditional")` — no Env scalar mocking at all. The 4 truth-table tests override via `deps.getEffectiveMetadataEnrichmentMode.mockResolvedValueOnce(...)`. No `Env.get*()` method closure risk present. ✓

### M5 — e2e-oauth-seed-as-browser: admin E2E uses withFreshContext correctly
**Rule:** `e2e-oauth-seed-as-browser.md`  
**File:** `apps/web/tests/e2e/specs-oauth/admin-metadata-enrichment-mode-aaa.spec.ts`  
**Status: PASS**

The spec does not seed user preferences (no `seedUserPreferences` calls). State manipulation is done via PATCH `/admin/settings` using `withFreshContext` + a fresh admin cookie (not the browser's page context), then the browser navigates and observes the result. This is the correct pattern — admin settings are global (single `app_config` row), not user-scoped, so no `seedAsBrowser` mechanic applies. ✓

The spec follows the `admin-timeframe-defaults-aaa.spec.ts` precedent exactly (same `mintAdminCookie` helper, same `withFreshContext` for PATCH calls, same `appShell.actions.navigateToRoute("/admin/settings")` for browser interaction). ✓

---

## LOW

### L1 — 400 validation test omits `body.error` assertion (consistent with file pattern)
**Rule:** `service-error-pattern.md` (scope-todo step 15)  
**File:** `apps/api/test/http/specs/admin-settings-aaa.http.spec.ts:362`  
**Status: LOW — no action required**

The `[metadata-enrichment-D]` test (invalid value `"foo"` → 400) calls only `statusIs(400)` without asserting `body.error`. The scope-todo said to check `error: "validation_error"`.

However, ALL 4 existing 400 validation tests for `repairCooldownMinutes` in the same file follow the identical `statusIs(400)`-only pattern (no body assertion). The KZO-189 addition is consistent. The `body.error` note in scope-todo is guidance on field naming when body IS asserted (`body.error` not `body.code`) — not a mandate to add body checks where none existed before.

**Recommendation:** Informational — consistent with file convention. If the team decides to standardize 400 body assertions across the file, that would be a separate refactor ticket.

---

## INFORMATIONAL

### I1 — BufferedEventBus unchanged
**Rule:** `buffered-eventbus-horizontal-scaling.md`  
`git diff dev...HEAD -- apps/api/src/events/` → empty. No changes to `BufferedEventBus`. ✓

### I2 — No daily-bar ticker seeding; ticker hygiene satisfied
**Rule:** `e2e-shared-memory-bars-ticker-hygiene.md`  
No `seedDailyBars`, `seedQuoteBars`, or `seed-daily-bars` calls in any new spec. ✓

### I3 — `shouldEnrich` placement: correctly after Zod parse, before try block
**Rule:** `typed-transient-error-catch-audit.md` § companion placement  
`const mode = await getEffectiveMetadataEnrichmentMode()` at line 149 and `const shouldEnrich` at line 150 are computed after the Zod parse at line 121 and before the main `try {` at line 182. A DB failure from mode resolution propagates uncaught to pg-boss (retry semantics — correct). ✓

### I4 — `setMetadataEnrichmentMode` bumps `_appConfigUpdatedAt`
`memory.ts:2241` calls `this._bumpAppConfigUpdatedAt()` after updating the field. Mirrors `setDashboardPerformanceRanges` at line 2231. ✓

### I5 — `console.warn` on missing `app_config` row mirrors existing precedent
`postgres.ts:5930` uses `console.warn("[app_config] row missing ...")` — identical to lines 5866 and 5887 for `getRepairCooldownMinutes`. Consistent with existing pattern. ✓

### I6 — `AdminSettingsClient-timeframes.test.tsx` impl-coupled extension correct
`apps/web/test/components/admin/AdminSettingsClient-timeframes.test.tsx:48–49` adds `metadataEnrichmentMode: null` and `effectiveMetadataEnrichmentMode: "conditional"` to the shared DTO fixture. Required for TypeScript compilation after `AppConfigDto` gained the two new required fields. ✓

---

## Mode Predicate Truth-Table Verification

Source: `backfillWorker.ts:149–150, 191`

```ts
const mode = await getEffectiveMetadataEnrichmentMode();
const shouldEnrich = mode === "unconditional" || trigger !== "daily_refresh";
// ...
provider.reserveCapacity(2 + (shouldEnrich ? 1 : 0));
```

| `mode` | `trigger` | `shouldEnrich` | `reserveCapacity` | Match |
|---|---|---|---|---|
| `unconditional` | `daily_refresh` | `true` (left arm) | 3 | ✓ |
| `conditional` | `user_selection` | `true` (right arm) | 3 | ✓ |
| `conditional` | `first_trade` | `true` (right arm) | 3 | ✓ |
| `conditional` | `retry` | `true` (right arm) | 3 | ✓ |
| `conditional` | `repair` | `true` (right arm) | 3 | ✓ |
| `conditional` | `daily_refresh` | `false` (both arms false) | 2 | ✓ |

Truth table fully satisfied. ✓

---

## Scope-Todo Item Coverage

| Step | Description | Status |
|------|-------------|--------|
| 1 | DB migration 045 | ✓ PASS |
| 2 | env-schema + env-metadata + .env.example | ✓ PASS |
| 3 | Persistence interface + memory + postgres | ✓ PASS |
| 4 | metadataEnrichmentMode.ts service module | ✓ PASS |
| 5 | AppConfigDto in shared-types | ✓ PASS |
| 6 | adminRoutes: schema + loadDto + PATCH handler | ✓ PASS |
| 7 | backfillWorker: shouldEnrich + reserveCapacity | ✓ PASS |
| 8 | pgBoss.ts wiring (not server.ts — design note honored) | ✓ PASS |
| 9 | Unit test mocks: createDeps + createAuDeps | ✓ PASS |
| 10 | AdminSettingsClient.tsx three-option select | ✓ PASS |
| 11 | Unit tests: 4 truth-table cases | ✓ PASS |
| 12 | Integration test: Postgres-backed CRUD + audit | ✓ PASS |
| 13 | Web unit test: AdminSettingsClient-metadataEnrichmentMode.test.tsx | ✓ PASS |
| 14 | E2E AAA: admin-metadata-enrichment-mode-aaa.spec.ts | ✓ PASS |
| 15 | HTTP AAA: admin-settings-aaa.http.spec.ts extended | ✓ PASS (LOW-L1: no body.error on 400, consistent with file) |
| 16 | Existing tests unchanged (verify compilation) | ✓ PASS (timeframes fixture extended correctly) |

Cross-task: TS2552 `createAuDeps` scope issue → **RESOLVED** (describe nested correctly at line 801).
