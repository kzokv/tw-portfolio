---
slug: kzo-155
source: scope-grill
created: 2026-04-21
tickets: [KZO-155]
required_reading: []
superseded_by: null
---

# Todo: KZO-155 — Extract sliding-window rate limiters

> **For agents starting a fresh session:** read all files listed in `required_reading` above before starting implementation.

## Implementation Steps

- [ ] Create `apps/api/src/lib/slidingWindowBucket.ts`
      Export: `sweepSlidingWindowBucket(bucket, windowMs, now?)`
      Move from `registerRoutes.ts` lines 962–972

- [ ] Create `apps/api/src/lib/inviteStatusRateLimit.ts`
      Move: `inviteStatusBuckets`, `INVITE_STATUS_WINDOW_MS = 60_000`, `INVITE_STATUS_LIMIT = 20`
      Export: `assertInviteStatusRateLimit(ip: string)` (signature change: was `req: FastifyRequest`, now `ip: string`),
              `_resetInviteStatusBuckets()`,
              `registerInviteStatusEviction(app: FastifyInstance)`
      Import `sweepSlidingWindowBucket` from `./slidingWindowBucket.js`

- [ ] Create `apps/api/src/lib/anonymousShareRateLimit.ts`
      Move: `anonymousShareRateBuckets`
      Export: `assertAnonymousShareRateLimit(ip: string)`,
              `_resetAnonymousShareRateBuckets()`,
              `deleteAnonymousShareRateBucket(ip: string)`,
              `registerAnonymousShareEviction(app: FastifyInstance)`
      Import `sweepSlidingWindowBucket` from `./slidingWindowBucket.js`
      Import `Env` from `@tw-portfolio/config` for window/limit values

- [ ] Update `registerRoutes.ts`
      - Remove all extracted symbols (buckets, consts, assert fns, reset fns, sweep fn)
      - Import and call `registerInviteStatusEviction(app)` + `registerAnonymousShareEviction(app)` at top of `registerRoutes()`
      - Call site `assertInviteStatusRateLimit`: change `assertInviteStatusRateLimit(req)` → `assertInviteStatusRateLimit(req.ip)` (line ~1594)
      - `/__e2e/anon-share-rate-reset` handler: replace `anonymousShareRateBuckets.delete(body.ip)` with `deleteAnonymousShareRateBucket(body.ip)`

- [ ] Update test imports
      - `test/unit/anonymous-share-rate-limiter.test.ts` → `../../src/lib/anonymousShareRateLimit.js`
      - `test/unit/rate-bucket-eviction.test.ts` → `../../src/lib/slidingWindowBucket.js`  *(missed by ticket)*
      - `test/integration/invites.integration.test.ts` → `../../src/lib/inviteStatusRateLimit.js`
      - `test/integration/admin-bootstrap.integration.test.ts` → `../../src/lib/inviteStatusRateLimit.js`

- [ ] Verify no remaining `registerRoutes.js` imports for extracted symbols
      `grep -r "inviteStatusBucket\|anonymousShareRate\|sweepSliding" apps/api/test --include="*.ts"`

- [ ] Run suites 2 (typecheck), 4 (API unit), 5 (integration) to confirm clean

## Out of Scope
- `adminRoutes.ts` — confirmed zero rate-limiter references; ticket claim was stale
- No new tests — all moved behavior already has coverage; only import paths change

## Open Items
(none)

## References
- Linear: KZO-155
