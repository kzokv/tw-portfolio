# Code Review — Phase 3c: shadcn Sidebar + Decomposed TopBar

**Date:** 2026-05-16 13:30  
**Reviewer:** code-reviewer (Tier 2 Phase 3 gate)  
**Commits reviewed:**
- `2911696` — `feat(web): adopt shadcn sidebar + decomposed TopBar (Phase 3c)` (Implementer)
- `dd1b06f` — `fix(test-e2e): AAA-compliant viewport detection in openSettingsDrawer` (QA)

**Verdict: FIX-REQUIRED** — 3 MEDIUM, 1 LOW

---

## Summary

Phase 3c delivers a substantial structural overhaul: shadcn `<Sidebar>` primitives replace the legacy `SideNavigation` + `AdminSidebar`, `ProfileMenu` replaces `UserAvatarButton`, `TopBar` is decomposed from `AppShell`, breadcrumb plumbing lands, and the SSR sidebar-cookie pipeline ships. The 17-item preservation checklist is largely satisfied. Three medium findings and one low finding require resolution before merge.

---

## Findings

### MEDIUM-1 — AppShell.tsx is 727 LOC (spec target ≤300)

**File:** `apps/web/components/layout/AppShell.tsx`  
**Rule:** Phase 3c spec §3c, architect-design §1 ("Target ≤300 LOC after extraction")

The file is 2.4× over target. The SettingsDrawer data layer — `useSettingsSave`, `settingsSave`, `transactionAccountOptions`, `handleRenameAccount`, `setDrawerOpen`, `navigationItems`, `quickSearchItems`, and associated state — was not extracted to the proposed `AppShellChrome.tsx` helper. All of this logic correctly belongs in AppShell for now (there is no regression), but the LOC spec target is documented in the Phase 3c spec as a hard deliverable, not a guideline.

**Fix:** Extract the SettingsDrawer data logic into `apps/web/components/layout/AppShellChrome.tsx` (or equivalent), reducing AppShell to the layout skeleton + prop-threading only. The extracted helper can export a hook or a context provider consumed by AppShell.

---

### MEDIUM-2 — TopBar.tsx is 457 LOC (spec target ≤200)

**File:** `apps/web/components/layout/TopBar.tsx`  
**Rule:** Phase 3c spec §3c, architect-design §1 ("Target ≤200 LOC")

The file is 2.28× over target. `QuickSearchPanel` and `SearchGroup` (internal sub-components) are defined in the same file and account for the bulk of the overage. These are not re-exported and have no dependencies that prevent extraction.

**Fix:** Extract `QuickSearchPanel` and `SearchGroup` into `apps/web/components/layout/QuickSearchPanel.tsx`. This is a pure move — no interface changes needed. TopBar would import and use them as before.

---

### MEDIUM-3 — `app/sharing/page.tsx` missing `initialSidebarOpen` (item 14 partial miss)

**File:** `apps/web/app/sharing/page.tsx` + `apps/web/app/sharing/layout.tsx`  
**Rule:** 17-item preservation checklist item 14; SSR sidebar-cookie pipeline (architect-design §4)

All page routes except `/sharing` pass `initialSidebarOpen` sourced from `readSidebarStateCookie()`. The sharing page uses a `"use client"` wrapper (`SharingRouteProvider`) with its own context. Neither `app/sharing/layout.tsx` (which is a server component) nor the page itself call `readSidebarStateCookie()` or thread `initialSidebarOpen` into AppShell.

Result: Users who have collapsed their sidebar will see it re-expand when navigating to the sharing page. On desktop this is a visible FOUC; the sidebar snaps from open → closed on first client paint. This is a real UX regression for users with a saved collapsed preference.

**Fix (two options, pick one):**

Option A — Add `initialSidebarOpen` in `app/sharing/layout.tsx`:
```ts
// apps/web/app/sharing/layout.tsx
import { readSidebarStateCookie } from "../../lib/sidebar-cookie";
export default async function SharingLayout({ children }: { children: React.ReactNode }) {
  const sidebarOpen = await readSidebarStateCookie();
  return <SharingRouteProvider initialSidebarOpen={sidebarOpen}>{children}</SharingRouteProvider>;
}
```
Then `SharingRouteProvider` and `SharingPage` thread `initialSidebarOpen` through to `AppShell`.

Option B — Call `readSidebarStateCookie()` directly in the server layout and inject it as a Next.js `searchParam`-style mechanism. This is more complex; Option A is preferred.

---

### LOW-1 — `avatar-menu-admin` alias is on the wrapper `<div>`, not on `<a>` (design intent mismatch)

**File:** `apps/web/components/profile/ProfileMenu.tsx` lines 233–240  
**Rule:** 17-item preservation checklist item 12; architect-design §7 ("the wrapper div carries the legacy `avatar-menu-admin` testid so existing OAuth specs that `getByTestId('avatar-menu-admin').click()` still hit a clickable element")

Current structure:
```tsx
<div data-testid="avatar-menu-admin">
  <DropdownMenuItem asChild>
    <a href="/admin" data-testid="profile-menu-admin-link">
```

The design comment in the spec says "existing OAuth specs that `getByTestId('avatar-menu-admin').click()` still hit a clickable element (the descendant `<a>`)." This is actually the correct interpretation — the div carries the legacy testid, the `<a>` carries the new canonical testid. A `.click()` on the div will bubble to the link child in Playwright. The functional behavior is preserved.

However, the architect-design §7 also says "The Admin entry carries both `data-testid='profile-menu-admin-link'` and the legacy `data-testid='avatar-menu-admin'`" (emphasis: carries BOTH on the same element). The spec's comment in the implementation (`ProfileMenu.tsx:231-233`) also references "the wrapper div carries the legacy testid so... `getByTestId('avatar-menu-admin').click()` still hit a clickable element (the descendant `<a>`)."

The current implementation matches the inline code comment's intent and works functionally. If any OAuth spec uses `getByTestId("avatar-menu-admin").click()` expecting the click to navigate (not just locate a div), the bubble will work. **No functional fix required** — but the Architect should confirm the inline comment is the authoritative intent and update the spec wording to avoid future reviewer confusion.

**Recommendation:** No code change. Architect to update architect-design §7 wording to clarify that the alias is intentionally on the wrapper div (not the same element as the canonical testid).

---

## 17-Item Preservation Checklist Audit

| # | Item | Status | Notes |
|---|------|--------|-------|
| 1 | `CONTEXT_FALLBACK_REVOKED_EVENT` window listener | ✅ PASS | AppShell.tsx line ~220-230; listener intact |
| 2 | `handleSharingNotification` + toast routing | ✅ PASS | AppShell.tsx; toast.info call preserved |
| 3 | `app-shell` testid on root | ✅ PASS | AppShell.tsx line 516 `data-testid="app-shell"` |
| 4 | `app-shell-ready` / `app-shell-client-ready` markers | ✅ PASS | Both present in AppShell.tsx |
| 5 | `switcher-data-ready` marker | ✅ PASS | AppShell.tsx; renders when portfolios loaded |
| 6 | `ImpersonationBanner` above sidebar | ✅ PASS | Rendered above `<SidebarProvider>` in both AppShell + AdminShell |
| 7 | globalError banner conditional | ✅ PASS | AppShell.tsx; `globalError` state + error.message display preserved |
| 8 | `clientApiErrorToast` | ✅ PASS | AppShell.tsx; `client-api-error` testid present |
| 9 | `useNotifications` with `enabled: true` | ✅ PASS | Hook defaults to `enabled: true`; AppShell passes `onSharingNotification` |
| 10 | `settingsSave` / `useSettingsSave` | ✅ PASS | Full SettingsDrawer logic preserved in AppShell |
| 11 | SSE pre-connect pattern | ✅ PASS | `enabled: true` confirmed in useNotifications.ts |
| 12 | `avatar-menu-admin` legacy alias | ✅ PASS (LOW-1) | Wrapper div pattern works; see LOW-1 for design-intent note |
| 13 | No `avatar-menu-settings` testid | ✅ PASS | Not present in ProfileMenu.tsx; removed from page objects |
| 14 | SSR `initialSidebarOpen` cookie | ⚠️ PARTIAL | All routes except `/sharing` — see MEDIUM-3 |
| 15 | `readSidebarStateCookie` uses `next/headers` | ✅ PASS | sidebar-cookie.ts line 2: `import { cookies } from "next/headers"` |
| 16 | Provider URL sanitization | ✅ PASS | `https://` check, `referrerPolicy="no-referrer"`, `onError` fallback in ProfileMenu.tsx |
| 17 | `breadcrumb-titles.ts` static map | ✅ PASS | Full map present; ADMIN_TITLES migrated; ordered longest-prefix-first |

---

## Decision Compliance

| Decision | Status | Notes |
|----------|--------|-------|
| #11 — No Settings in ProfileMenu | ✅ PASS | No Settings link; `avatar-menu-settings` absent |
| #21 — breadcrumb-titles.ts | ✅ PASS | `BREADCRUMB_FALLBACK_MAP` + `resolveBreadcrumbTitle()` exported |
| #22 — ⌘K placeholder only | ✅ PASS | `CommandPaletteTrigger.tsx`: `toast.info("Command palette coming in Phase 3e.")` |
| #23 — PortfolioSwitcher in SidebarHeader | ✅ PASS | AppSidebar.tsx: switcher slot BELOW brand link in `<SidebarHeader>` |

---

## Locked Testid Contract Audit

Per `playwright-page-object-testid-drift.md` — all new testid strings verified against source:

| Testid | Source File | Status |
|--------|-------------|--------|
| `app-sidebar` | AppSidebar.tsx `<Sidebar>` | ✅ |
| `app-sidebar-trigger` | TopBar.tsx `<SidebarTrigger>` | ✅ |
| `app-sidebar-brand` | TopBar.tsx mobile button; AppSidebar.tsx desktop link (`!isMobile`) | ✅ |
| `app-sidebar-rail` | AppSidebar.tsx, admin variant only | ✅ |
| `app-sidebar-nav-{key}` | AppSidebar.tsx nav items | ✅ |
| `app-sidebar-portfolio-switcher-slot` | AppSidebar.tsx wrapper | ✅ |
| `topbar` | TopBar.tsx `<header>` | ✅ |
| `topbar-breadcrumb` | TopBar.tsx wrapper div | ✅ |
| `topbar-search-input` | TopBar.tsx desktop input | ✅ |
| `topbar-search-button` | TopBar.tsx mobile toggle | ✅ |
| `topbar-search-sheet` | TopBar.tsx mobile sheet | ✅ |
| `topbar-search-sheet-input` | TopBar.tsx | ✅ |
| `topbar-search-results` | QuickSearchPanel `dataTestId` | ✅ |
| `topbar-search-sheet-results` | QuickSearchPanel `dataTestId` | ✅ |
| `topbar-command-trigger` | CommandPaletteTrigger.tsx | ✅ |
| `topbar-theme-toggle` | TopBar.tsx wrapper | ✅ |
| `topbar-profile-menu-trigger` | ProfileMenu.tsx `<Button>` | ✅ |
| `profile-menu-content` | ProfileMenu.tsx `<DropdownMenuContent>` | ✅ |
| `profile-menu-header` | ProfileMenu.tsx `<DropdownMenuLabel>` | ✅ |
| `profile-menu-profile-link` | ProfileMenu.tsx profile item | ✅ |
| `profile-menu-theme-light/system/dark` | ProfileMenu.tsx theme items | ✅ |
| `profile-menu-admin-link` | ProfileMenu.tsx `<a>` | ✅ |
| `avatar-menu-admin` | ProfileMenu.tsx wrapper `<div>` | ✅ |
| `profile-menu-sign-out` | ProfileMenu.tsx sign-out link | ✅ |
| `topbar-notification-bell` | NotificationBell.tsx wrapper | ✅ |
| `notification-bell-button` | NotificationBell.tsx PopoverTrigger | ✅ |
| `notification-bell-unread-count` | NotificationBell.tsx badge | ✅ |
| `topbar-notification-popover` | NotificationBell.tsx PopoverContent | ✅ |
| `notification-popover-content` | NotificationDropdown.tsx inner div | ✅ |
| `notification-mark-all-read` | NotificationDropdown.tsx | ✅ |
| `notification-empty-state` | NotificationDropdown.tsx | ✅ |
| `breadcrumb-root` | Breadcrumb.tsx `<nav>` | ✅ |
| `app-shell-ready` | AppShell.tsx | ✅ |
| `app-shell` | AppShell.tsx root | ✅ |

Page-object alignment (QA commit `dd1b06f`):
- `AppShellPage.ts` — all new locators verified against source testids ✅
- `TopBarComponent.ts` — all new locators verified; `avatar-menu-settings` absent ✅
- `SideNavigationComponent.ts` — all `app-sidebar-nav-{key}` patterns verified ✅

---

## Per-Rule Audits

### `react-useEventStream-preconnect-pattern`
✅ PASS — `useNotifications` hook has `enabled: true` as default at line 82 of `useNotifications.ts`. AppShell passes `onSharingNotification` callback; pre-connect preserved.

### `provider-url-sanitization`
✅ PASS — ProfileMenu.tsx:
- Line 97-98: `pictureUrl && pictureUrl.startsWith("https://") && !imgError ? pictureUrl : null`
- Line 127: `referrerPolicy="no-referrer"`
- Line 128: `onError={() => setImgError(true)}`

### `nextjs-server-cookie-access`
✅ PASS — `apps/web/lib/sidebar-cookie.ts` imports `{ cookies } from "next/headers"` and `await cookies()`. No `document.cookie` usage.

### `responsive-dual-layout-testid-prefixes`
✅ PASS — No dual-layout table+card pattern introduced in this PR. The mobile vs desktop `app-sidebar-brand` split (TopBar button vs AppSidebar link) uses conditional rendering (`lg:hidden` / `isMobile`), not simultaneous DOM presence, so strict-mode duplicate testid does not apply.

### `playwright-page-object-testid-drift`
✅ PASS — All page-object locator strings verified against source `data-testid` attributes (see table above).

### `commit-format`
✅ PASS — Both commits are on the waiver track (no KZO-XX prefix):
- `feat(web): adopt shadcn sidebar + decomposed TopBar (Phase 3c)` — type/scope/subject format correct
- `fix(test-e2e): AAA-compliant viewport detection in openSettingsDrawer` — type/scope/subject format correct

### `new ResizeObserver` grep
✅ PASS — Grepped `apps/web/components/layout/`. No `new ResizeObserver` found. Removed cleanly.

---

## File Deletion Verification

The following legacy files were deleted in commit `2911696`:
- ✅ `apps/web/components/layout/SideNavigation.tsx` — deleted
- ✅ `apps/web/components/admin/AdminSidebar.tsx` — deleted  
- ✅ `apps/web/components/layout/UserAvatarButton.tsx` — deleted

---

## SSR Cookie Pipeline Completeness

| Route | `readSidebarStateCookie()` | `initialSidebarOpen` passed | Status |
|-------|---------------------------|----------------------------|--------|
| `/dashboard` | ✅ | ✅ | PASS |
| `/portfolio` | ✅ | ✅ | PASS |
| `/transactions` | ✅ | ✅ | PASS |
| `/cash-ledger` | ✅ | ✅ | PASS |
| `/dividends` | ✅ | ✅ | PASS |
| `/dividends/review` | ✅ | ✅ | PASS |
| `/tickers` | ✅ | ✅ | PASS |
| `/admin/*` layout | ✅ | ✅ | PASS |
| `/sharing` | ❌ | ❌ | **MEDIUM-3** |

---

## QA Commit Specific Review (`dd1b06f`)

`fix(test-e2e): AAA-compliant viewport detection in openSettingsDrawer`

The fix replaces viewport-mode string logic with Playwright's native `page.viewportSize()` / `isMobile` fixture check. This is correct — it removes the dependency on the retired `viewportMode` state that lived in the old AppShell. Page-object locator updates (`desktopNavToggle`, `mobileSidebar`, `desktopSidebar`, `mobileNavToggle`, breadcrumb locators) are all correctly mapped to the new testids.

No findings in the QA commit.

---

## Stale-Artifact Confirmation

All greps scoped to `apps/`, `libs/`, `.claude/` source paths only. No hits from `.next/` or `dist/` directories included.

---

## Fix Priority Summary

| ID | Severity | File | Action Required |
|----|----------|------|-----------------|
| MEDIUM-1 | MEDIUM | `AppShell.tsx` (727 LOC vs ≤300) | Extract SettingsDrawer data logic to `AppShellChrome.tsx` |
| MEDIUM-2 | MEDIUM | `TopBar.tsx` (457 LOC vs ≤200) | Extract `QuickSearchPanel` + `SearchGroup` to `QuickSearchPanel.tsx` |
| MEDIUM-3 | MEDIUM | `app/sharing/page.tsx` | Add `readSidebarStateCookie()` in `app/sharing/layout.tsx`; thread to AppShell |
| LOW-1 | LOW | `ProfileMenu.tsx` / spec | No code change; Architect to clarify spec wording on alias placement |
