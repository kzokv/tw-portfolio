# Phase 3a + 3b Code Review
Date: 2026-05-16
Commits: 4e5ca08 (Phase 3a) + 97eff19 (Phase 3b)
Reviewer: code-reviewer

## Verdict: CLEAN

---

## Checklist Results

### (1) Hook ownership matrix
✅ All hooks match the design matrix exactly. `useDashboardData`, `useTransactionSubmission`, `useTransactionMutations`, `useRecomputeAction`, `useProfile`, `useNotifications`, `useSettingsSave`, `useEffectiveRanges`, `useSharedContextOwnerId` all remain in AppShell. `useDashboardPerformance` moved to DashboardClient with `enabled: true`. `useRecentTransactions` moved to TransactionsClient with `enabled: true`. PortfolioClient has no page-scoped hook move (correct — only uses `dashboard.*` and `mutations.*` from context which refresh via AppShell's `dashboard.refresh()`).

### (2) Providers add ZERO new DOM nodes
✅ Both `AppShellDataProvider` and `CardLayoutResetProvider` render `<Ctx.Provider value={value}>{children}</Ctx.Provider>` with no wrapper divs. Verified in `AppShellDataContext.tsx:63` and `CardLayoutResetContext.tsx:22`.

### (3) Skeleton-gate parity
✅ All three Clients render `<DashboardLoading />` + skeleton-line bar when `isBootstrapping || !isI18nReady` (sourced via `useAppShellData()`). DashboardClient: lines 59–66. PortfolioClient: lines 24–31. TransactionsClient: lines 42–49. Pattern is identical across all three. AppShell `showPageSkeleton` correctly deleted — now unreachable since all pages pass children.

### (4) Preserved properties
✅ `section` prop present on AppShell (interface line 54, default `"dashboard"` at line 88), still used for `derivedShellTitle`, `derivedShellDescription`, and `activeSectionOverride ?? section` in SideNavigation. SettingsDrawer NOT moved — still imported and rendered from AppShell (lines 15, 878). No `DividendsClient.tsx` created.

### (5) cardLayoutResetCounts setter wiring
✅ AppShell owns `useState<{dashboard, transactions, portfolio}>` (line 117). `setCardLayoutResetCounts` called directly in `onLayoutReset` (line 899) and `onPageLayoutReset` (line 906) on SettingsDrawer. Context exposes read path only (`CardLayoutResetProvider value={cardLayoutResetCounts}`). Each Client reads via `useCardLayoutResetCount(page)`.

### (6) Status banners in AppShell
✅ All five testids present in AppShell `<main>` BEFORE `{children}`, in correct order:
- `global-error-banner` (line 783)
- `transaction-status` (line 811)
- `mutation-status` (line 827)
- `recompute-status` (line 838)
- `snapshot-status` (line 849)
`ApiClientErrorToast` and `StatusToast` also present before banners (lines 776–777).

### (7) §8 behavioral preservation
✅ All items confirmed present:
- `CONTEXT_FALLBACK_REVOKED_EVENT` listener: AppShell lines 311–319, window.addEventListener / removeEventListener pattern intact
- `?as=ownerId` deep-link guard with `deepLinkAppliedRef` and `window.history.replaceState`: lines 134, 282–296
- PortfolioSwitcher integrated: line 722 in TopBar section
- Shared-context dict mutation: `hasOwnerEmptyPortfolio` (line 160), `uiDict` useMemo with dashboardHome override still present; `transactions` override is unconditional in shared context (see §8b below)
- `router.refresh()` on context change: line 243 inside `refreshContextDependentData`
- ImpersonationBanner above TopBar: line 680
- ApiClientErrorToast mount: line 776
- StatusToast mount: line 777
- Sonner Toaster: in `apps/web/app/layout.tsx` (was never in AppShell; already correct)
- Theme toggle in TopBar: not moved in 3b (3b is zero-visual-change; ThemeToggle is a TopBar responsibility, unchanged)
- `useNotifications` with `enabled: true`: AppShell line 450 calls the hook; the hook itself has `enabled: true` hardcoded at `apps/web/hooks/useNotifications.ts:82`
- SSE pre-connect pattern intact: confirmed by `enabled: true` in `useNotifications`

### (8a) contextRefreshSignal deviation
✅ `contextRefreshSignal` counter present in AppShellDataContext (line 51) and AppShell state (line 240). `firstSignalRef` skip-initial-mount pattern present in both DashboardClient (lines 43–50) and TransactionsClient (lines 33–40). All 6 trigger sites confirmed:
  1. `refreshContextDependentData` → context-cookie change handler (line 244)
  2. `refreshAfterTransaction` → transaction submit callback (line 352)
  3. `refreshAfterRecompute` → recompute confirm callback (line 366)
  4. `onSnapshotsDone` → snapshot generation callback (line 397)
  5. global-error retry onClick handler → line 798
  6. `onReportingCurrencySaved` → SettingsDrawer reporting-currency save (line 919)
PortfolioClient correctly does NOT subscribe (its data comes from `dashboard.*` which AppShell refreshes directly).

### (8b) hasOwnerEmptyRecentTransactions length-check dropped
✅ `hasOwnerEmptyRecentTransactions` is dropped. `dict.transactions.recentLedgerEmpty` is now unconditionally overridden in shared context (AppShell lines 181–183). Claim of behavior-identical is verified: `recentLedgerEmpty` is only consumed in `RecentTransactionsCard.tsx:42` inside the `items.length === 0` branch (empty-state only). When `items.length > 0` the empty branch doesn't render, so the unconditional override is harmless. The inline comment at AppShell line 163 documents this correctly.

### (9) No spurious JSX restructuring
✅ Only documented extractions performed: `renderSection` deleted, `RouteHeroPanel` and `StatusStripCard` moved to `SectionHeroPanels.tsx`. No JSX was restructured within AppShell's own render tree beyond adding the provider wrappers. No test files changed across either commit.

---

## Findings

None.

---

## Sign-off

Phase 3a and 3b are structurally correct and fully aligned with `architect-design.md`. The shadcn primitives install (3a) touches only new files, globals.css, tailwind config, and package files — no call-site changes, confirming zero regression risk. The `renderSection` extraction (3b) cleanly separates page render trees into DashboardClient, PortfolioClient, and TransactionsClient while preserving all shell-owned state (banners, SettingsDrawer, mutations, notifications, switcher, impersonation). The `contextRefreshSignal` counter is a correct and complete substitute for the previous `section === "dashboard" ? performance.refresh() : Promise.resolve()` conditional fan-out pattern, and all 6 trigger sites are wired. Both providers are DOM-invisible. The skeleton gate is faithfully replicated in each Client. No behavioral deviations detected. Recommend proceeding to Phase 3c.
