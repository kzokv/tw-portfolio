name: vakwen-local

services:
  vakwen-local-postgres:
    image: postgres:16
    container_name: vakwen-local-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vakwen}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-vakwen}
    volumes:
      - pgdata_local:/var/lib/postgresql/data
    networks:
      - vakwen-local-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vakwen} -d ${POSTGRES_DB:-vakwen}"]
      interval: 10s
      timeout: 5s
      start_period: 20s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    ports:
      - "5732:5432"

  vakwen-local-redis:
    image: redis:7
    container_name: vakwen-local-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    networks:
      - vakwen-local-net
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH=$$REDIS_PASSWORD redis-cli ping"]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 5
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    ports:
      - "6679:6379"

  vakwen-local-migrate:
    profiles: ["migrate"]
    build:
      context: ../..
      dockerfile: db/Dockerfile.migrate
    image: vakwen-local-migrate:${IMAGE_TAG:-latest}
    container_name: vakwen-local-migrate
    depends_on:
      vakwen-local-postgres:
        condition: service_healthy
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGHOST: vakwen-local-postgres
      PGUSER: ${POSTGRES_USER:-vakwen}
      PGDATABASE: ${POSTGRES_DB:-vakwen}
    networks:
      - vakwen-local-net
    restart: "no"
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"

  vakwen-local-api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    image: vakwen-local-api:${IMAGE_TAG:-latest}
    container_name: vakwen-local-api
    restart: unless-stopped
    depends_on:
      vakwen-local-postgres:
        condition: service_healthy
      vakwen-local-redis:
        condition: service_healthy
    env_file:
      - ${APP_ENV_FILE:-.env.local}
    environment:
      # 'test' avoids Secure cookie flag (production) and port validation mismatch (development)
      NODE_ENV: ${NODE_ENV:-test}
      DB_URL: postgres://${POSTGRES_USER:-vakwen}:${POSTGRES_PASSWORD}@vakwen-local-postgres:5432/${POSTGRES_DB:-vakwen}
      REDIS_URL: redis://:${REDIS_PASSWORD}@vakwen-local-redis:6379
      API_PORT: "4000"
      # Host ports are fixed at +300 offset from container ports (3000→3300,
      # 4000→4300) to avoid collision with host-level dev servers.
      ALLOWED_ORIGINS: http://localhost:3300
      APP_BASE_URL: http://localhost:3300
      # Overrides env_file — must use host-side port (4300), not container port (4000)
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:4300/auth/google/callback}
      SESSION_COOKIE_NAME: ${SESSION_COOKIE_NAME:-g_auth_session}
    networks:
      - vakwen-local-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:4000/health/live || exit 1"]
      interval: 15s
      timeout: 5s
      start_period: 30s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    ports:
      - "4300:4000"

  vakwen-local-web:
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: http://localhost:4300
        NEXT_PUBLIC_AUTH_MODE: ${AUTH_MODE:-oauth}
    image: vakwen-local-web:${IMAGE_TAG:-latest}
    container_name: vakwen-local-web
    restart: unless-stopped
    depends_on:
      vakwen-local-api:
        condition: service_healthy
    environment:
      # 'test' avoids Secure cookie flag (production) and port validation mismatch (development)
      NODE_ENV: ${NODE_ENV:-test}
      HOSTNAME: "0.0.0.0"
      PORT: "3000"
      # Server-side API base URL for route handlers running inside the container network.
      SERVER_API_BASE_URL: http://vakwen-local-api:4000
      SESSION_COOKIE_NAME: ${SESSION_COOKIE_NAME:-g_auth_session}
      SESSION_SECRET: ${SESSION_SECRET:?required}
      # Build-time NEXT_PUBLIC_* args are NOT inherited by the Dockerfile runtime
      # stage; the web server (proxy.ts, auth.ts) reads process.env at runtime.
      NEXT_PUBLIC_AUTH_MODE: ${AUTH_MODE:-oauth}
      DEMO_MODE_ENABLED: ${DEMO_MODE_ENABLED:-false}
    networks:
      - vakwen-local-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/ || exit 1"]
      interval: 15s
      timeout: 5s
      start_period: 20s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    ports:
      - "3300:3000"

volumes:
  pgdata_local:

networks:
  vakwen-local-net:
    driver: bridge
